package main

import "fmt"

func main() {
	array := [5]int{1, 2, 3, 4, 5}
	fmt.Println(array)
}

//Não é muito usado, mas basicamente é estruturado dessa forma
//Os valores que estão nas chaves são só demonstrativos sobre os que podem ficar armazenados
//nos cinco "campos de memória" que reservei para esse array
